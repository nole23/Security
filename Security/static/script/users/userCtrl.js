
'use strict';

angular.module('simeCenterApp')
	.controller('UserCrtl', ['$scope', '$localStorage', '$http', '$window', '$log', '_', '$rootScope', 'UserResource',
	    function($scope, $localStorage, $http, $window, $log, _, $rootScope, UserResource) {
		
		
		
	}]);